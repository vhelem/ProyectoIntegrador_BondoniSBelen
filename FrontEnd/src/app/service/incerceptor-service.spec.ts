import { IncerceptorService } from './incerceptor-service';

describe('IncerceptorService', () => {
  it('should create an instance', () => {
    expect(new IncerceptorService()).toBeTruthy();
  });
});
